析构函数：可以理解为程序要清除该类对象之前最后一次希望该对象完成的工作（一般为清理工作）。
	 切忌不可认为析构函数就是清除对象的过程！
	 完成析构函数体之后，才开始真正清除该对象（也许只是弹出栈，而该内存的数据并没有变化，只不过栈可以再利用），清除工作程序自动完成（基本类型，成员类对象，成员指针等等）
    	 因此普通成员变量不需要显示在析构函数中调用他们的析构函数（清除）
	 但是对于new的内存，一般如果没有转移该堆内存的控制权，必须在析构函数中，delete这块内存，之后程序自动清除（弹出栈）指针变量。